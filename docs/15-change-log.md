# 15 — Change Log

Every material change to the project or its documentation (CL-###). Newest
first.

---

| ID | Date | What | Why | Affected docs | Impact |
|----|------|------|-----|---------------|--------|
| CL-018 | 2026-08-13 | **SPRINT-07 (EPIC-010/011 — History + Versioning + Hardening) opened and planned**: `04-sprints.md` SPRINT-07 → ACTIVE (start 2026-08-13, target end 08-15, capacity M) with phase plan A–G (A: CI gate `mvn verify` TASK-087 — suite moves onto a shared baseline first; B: version increment + immutable versions TASK-083/084; C: source-of-truth enforcement TASK-084 (no chaining — `analysis_id` + master `resume_version_id` stays, deviations recorded); D: analysis history endpoints TASK-085 + history UI TASK-086; E: integration test fixtures TASK-088; F: global error handling + retry UX TASK-089/090; G: security checklist §12.1 + AI transparency UI TASK-091/092 → close CL-019 (CL-017 reserved for the SPRINT-06 close, CL-018 is this open row)); FEAT-044..048 + TASK-083..092 → READY in `05-backlog.md`/`06-technical-tasks.md`, counts regenerated (9 EPICs / 43 FEATs / 82 TASKs DONE, 5 FEATs / 10 TASKs READY, 0 PLANNED left in 06); backend and frontend untouched (pure planning). | Open the hardening sprint per the roadmap after SPRINT-06 closed: CI gate, immutability + history, error handling, security/privacy before any advanced work | `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `15-change-log.md` | SPRINT-07 ACTIVE with 10 READY tasks; implementation starts with TASK-087 (CI gate). |
| CL-017 | 2026-08-13 | **SPRINT-06 (EPIC-008/009 — Review + PDF/DOCX/JSON Export) implemented and verified**: `ChangeReviewService` (review actions `accept\|reject\|edit\|regenerate`; accept/reject only on PENDING, **edit additionally on EDITED/REGENERATED — re-edit path avoids the re-generation deadlock (locked deviation)**, first review action moves READY → NEEDS_REVIEW, edit requires non-blank `new_text`, edit/regenerate revalidate in place); `POST /tailored/{id}/changes/{changeId}` (+ `ReviewRequest` with `@JsonProperty("action")`/`@JsonProperty("new_text")` — snake_case body per the §5.8 contract) and `POST /tailored/{id}/approve` → `{"status":"approved"}` (idempotent for APPROVED; 409 while any change is PENDING or the persisted validation report is not `valid=true`); `ExportService` — export gate `assertExportable` (state ∈ READY\|NEEDS_REVIEW\|APPROVED AND no PENDING changes AND validation `valid=true` else 409; 404 unknown id), Thymeleaf `templates/export/resume.html` rendered by a dedicated `exportTemplateEngine` bean (SpringTemplateEngine, prefix `templates/export/`, suffix `.html` — name `"resume"`); PDF via Flying Saucer `ITextRenderer` — dependency relocated to **`org.xhtmlrenderer:flying-saucer-pdf:9.4.0`** (the `flying-saucer-pdf-openpdf` 9.11.3 coordinate does not exist on Maven Central — documented); DOCX via Apache POI XWPF with `• ` bullets; artifacts under `ats.doctor.storage.exports-dir` (`${ATS_DOCTOR_EXPORTS_DIR:data/exports}`, `<id>.pdf`/`<id>.docx`), `html`/`pdf_path`/`docx_path` persisted on `tailored_resumes`; `GET /tailored/{id}/export/{pdf|docx|json}` (JSON reuses the `TailoredResponse` shape; unknown format → 400 via `TailoringExceptionHandler` widened to cover `TailoringExportController`); REJECTED changes skipped by `ValidationService` (never ship) and exported as their original text (locked deviation — reject-all no longer deadlocks approval); ValidationService guard admits VALIDATING/READY/NEEDS_REVIEW (APPROVED → 409). Frontend: `ReviewPanel.tsx` client component (accept/reject/edit/regenerate with inline textarea, approve button with pending count, PDF/DOCX/JSON export links, error/notice banners) + server-fetched `page.tsx` with state badges (READY/NEEDS_REVIEW/APPROVED); `ChangeList.tsx` replaced. Tests: `TailoringReviewFlowTest` (Testcontainers round-trip: tailor → review actions → approve → all three exports; guarded 409/400/404 paths; reject-all → valid report → approve → export; asserts PDF `%PDF` and DOCX `PK` magic and persisted `pdf_path`/`docx_path` under `target/test-review-exports`) + `TailoringExportControllerTest`; `TailoringControllerTest`/`TraceabilityControllerTest`/`ValidationControllerTest` gained `@MockitoBean ChangeReviewService`; debug probe test files deleted. **237/237 backend tests green** (Testcontainers with OrbStack `DOCKER_HOST`), frontend `next build` green. Marked EPIC-008/009, FEAT-037..043, TASK-074..082 DONE; SPRINT-06 COMPLETED with completed_date 2026-08-13 + closed notes (deviations, dependency facts, ValidationService guard extension); counts 9 EPICs / 43 FEATs / 82 TASKs DONE. | Ship the review + export sprint: per-change review lifecycle, approval gate, and ATS-ready file exports per PRD §8.2 | `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `15-change-log.md`, `backend/*`, `frontend/*` | SPRINT-06 DONE: 9 EPICs / 43 FEATs / 82 TASKs DONE; review + export ready for SPRINT-07 hardening. |
| CL-016 | 2026-08-13 | **SPRINT-06 (EPIC-008/009 — Review + PDF/DOCX Export) opened and planned**: `04-sprints.md` SPRINT-06 → ACTIVE (start 2026-08-13, target end 08-15, capacity M) with head-start note (SPRINT-05 already shipped the `ChangeList` + `TraceDrawer` under `/tailored`; the read-only GET side of TASK-074 is done — SPRINT-06 adds the POST lifecycle) and locked decisions (TailoringState gains `APPROVED` — still no FAILED per §5.8; change `status` PENDING → ACCEPTED\|REJECTED\|EDITED\|REGENERATED via `StateMachines`; EDITED/REGENERATED re-run validation before re-approval; export → 409 until all changes resolved AND warnings cleared, PRD §8.2; exports render the exact `tailored_resumes` content — no chaining §7.6; `html`/`pdf_path`/`docx_path` populated); phase plan A–G deepened (B: POST change actions + regenerate re-run + revalidation; C: review controls + before/after diff on the existing page; D: approve → APPROVED + 409 gate; E: Thymeleaf sanitized ATS template; F: Flying Saucer + OpenPDF with fidelity checks; G: POI XWPF — already a dependency — + `export/pdf|docx|json` + Testcontainers export test); close renumbered to CL-017 (CL-016 supersedes the earlier "close as CL-016" wording); EPIC-008/009 IN_PROGRESS, FEAT-037..043 + TASK-074..082 READY (`06` blocks flipped + TASK-074/076/077 `files_or_modules` corrected to the shipped `frontend/app/tailored/`), counts regenerated (7 EPICs / 36 FEATs / 73 TASKs DONE, 2 EPICs IN_PROGRESS, 9 TASKs READY); 13 regenerated (§2 ACTIVE row, §3 in-progress, §4 Testcontainers/OrbStack run note, §5 next up = SPRINT-07); backend and frontend untouched (pure planning). | Open the review + export sprint per the roadmap: per PRD §8 the approval gate and exports are the last user-facing MVP flow before hardening | `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `15-change-log.md` | SPRINT-06 ACTIVE with 9 READY tasks; implementation starts with TASK-074 (POST change actions). |
| CL-015 | 2026-08-13 | **SPRINT-05 (EPIC-007 — Fact Validation + Claim Traceability) implemented and verified**: `DeterministicValidator` (category-A facts — technology/metric; category-B descriptors; C-claim achievement variant, no LLM), `fact_validation` task + `validator-v1` prompt (`AiValidator`, one `ai_runs` row per call), token-identity output normalization/dedupe, `ValidationRules` typed `Verdict` merge (stable fact→descriptor→claim order); `VALIDATING` now runs for real — `TailoringPipeline` invokes `ValidationService.validate` (V6: `validation` JSONB on `tailored_resumes`; state guard VALIDATING/READY; stuck-VALIDATING completes to READY; revalidation allowed); APIs: `POST /tailored/{id}/validate` (typed issues + `checked_changes` + `validated_at`), `GET /tailored/{id}/changes` (read-only listing — GET side of TASK-074 pulled into SPRINT-05 for the drawer; POST lifecycle stays in SPRINT-06), `GET /tailored/{id}/changes/{change_id}/trace` (change → evidence chain/`source_refs` → section context (company/title) → tailored bullet → its validation issues; 404 guards); frontend: `/tailored` index + id-lookup form, `/tailored/[id]` (score cards, state badge, change list), "Why is this claim here?" `TraceDrawer`, home link live; `next build` green. **Two bugs found by Testcontainers and fixed**: (1) `TailorDecider` passed the bullet id as `evidence_id` → every `tailored_changes` row NULL and traces empty — pipeline now resolves section id → evidence row UUID (`evidenceIdFor`) and `TraceabilityService.sectionOf` resolves bullet evidence to its experience entry; (2) issue `type` emitted as uppercase enum name, violating the §5.8 contract — normalized to lowercase `unsupported_fact\|unsupported_descriptor\|unsupported_claim` (tests updated). Tests: 2 new Testcontainers methods (refresh → tailor → validate → typed issues → changes listing → per-change trace + 404s) — **226/226 green with Testcontainers enabled** (local runs need `DOCKER_HOST=unix:///Users/azhar/.orbstack/run/docker.sock` + `-Dapi.version=1.41`; `/var/run/docker.sock` is a stale symlink). Marked EPIC-007, FEAT-033..036, TASK-067..073 DONE (+ stale TASK-059..065 READY rows in 05 corrected to DONE); SPRINT-05 COMPLETED; counts 7 EPICs / 36 FEATs / 73 TASKs DONE. Close is CL-015 (the "close as CL-014" in the phase plan was superseded — CL-014 was the planning row). | Ship the validation + traceability sprint: every claim is checked in three stages and traces to its evidence, per 04-sprints exit criteria | `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `15-change-log.md`, `backend/*`, `frontend/*` | SPRINT-05 DONE: 7 EPICs / 36 FEATs / 73 TASKs DONE; `VALIDATING` + trace APIs ready for SPRINT-06 review/export. |
| CL-014 | 2026-08-13 | **SPRINT-05 (EPIC-007 — Fact Validation + Claim Traceability) started and planned**: yaml ACTIVE (2026-08-13 → 08-14, capacity M, exit criteria extended — `VALIDATING` runs for real, 171/171 stays green); scope note (three-stage validation per §5.8: deterministic TASK-067 → AI TASK-068/069 → typed rule engine TASK-070; review UI is FEAT-037/SPRINT-06); phase plan A–G drives TASK-067..073 in dependency order (A context/contracts, B deterministic validator, C AI validator + normalization, D rule engine A/B/C, E `POST /tailored/{id}/validate`, F `source_refs` traceability API, G traceability UI + verification); EPIC-007 IN_PROGRESS, FEAT-033..036 + TASK-067..073 READY, 06 blocks flipped READY, counts now 6 EPIC / 32 FEAT / 66 TASK DONE with 4 FEAT / 7 TASK READY; 13 backfilled (§2 ACTIVE row, §3 in-progress, §5 next up = SPRINT-06); backend left untouched (pure planning). | Open the next sprint — validation now has a real `VALIDATING` phase and every claim gets a `source_refs` chain | `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `15-change-log.md` | SPRINT-05 ACTIVE; implementation starts with TASK-067. |
| CL-013 | 2026-08-13 | **SPRINT-04 (EPIC-006 — AI Tailoring) implemented and verified**: `V5__create_tailoring_tables.sql` (`tailored_resumes` per PRD §7.7 + denormalized `resume_version_id` (no-chaining §7.6) + `state`/`error`/`updated_at` precedent; `html`/`pdf_path`/`docx_path` NULL until SPRINT-06; `tailored_changes` per §7.8 with nullable `evidence_id`); entities/repos `TailoredResume`/`TailoredChange`; `application/tailoring/` (`TailoringService` — READY-analysis guard, one active run per analysis, QUEUED→GENERATING→VALIDATING→READY via `StateMachines.tailoring()`, change rows PENDING with prompt_version, failure via `error` — TailoringState has no FAILED per §5.8; async `TailoringPipeline` (TransactionTemplate so lazy entities load off the request thread) — TailorDecider → per-bullet `ai.generate(task='resume_tailoring')` with `tailor-bullet-v1` A/B/C prompt (one `ai_runs` row per call) → summary rewrite → reorder → deterministic `score_after` (MatchPipeline + ScoreCalculator over tailored content) → persist); `infrastructure/tailoring/` (`TailorDecider` — gap-driven bullet selection, skips covered, cap 3, no-op safe; `BulletRewriter` — tolerant JSON/plain/fence parsing, clip limits; `SummaryRestructurer` — citation-weighted stable reorder); `StubAiProvider` deterministic grounded rewrite (first JD keyword supported by resume evidence but omitted by the bullet → “, with a focus on …”, A/B/C-safe, `_stub` labeled); `api/tailor/` (`POST /analyses/{id}/tailor`, `GET /tailored/{id}`, snake_case `TailoredResponse` with re-parsed content, ProblemDetail 400/404/409). Tests: 30 new (171 total, all green) incl. `TailoringPipelineIntegrationTest` (Testcontainers: resume+JD → analysis READY → tailor → READY, rewritten bullets, PENDING change rows, `ai_runs.task=resume_tailoring` rows, GET round-trip, error mappings). Docs reconciled (03 feature cards rebuilt; 04 COMPLETED with plan-text corrections — VALIDATING pass-through, no FAILED, deterministic score_after; 05 EPIC-006/FEAT-028..032/TASK-059..066 DONE + counts 6/32/66; 06 eight blocks DONE; 07 tailor rows updated; 08 V1..V5 + tailoring tables; 13 regenerated). E2E on docker-compose NOT run — user verifies manually (no compose build in-session). | Ship the AI Tailoring sprint: selective rewriting of gap bullets + summary, grounded evidence, persisted changes + deterministic re-score, per the 04-sprints exit criteria | `03-features.md`, `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `07-api-contract.md`, `08-database-schema.md`, `13-project-status.md`, `15-change-log.md`, `backend/*` | SPRINT-04 DONE: 6 EPICs / 32 FEATs / 66 TASKs DONE; tailor API ready for SPRINT-05 validation. |
| CL-012 | 2026-08-13 | **SPRINT-04 (EPIC-006 — AI Tailoring) started and planned**: `04-sprints.md` SPRINT-04 → ACTIVE (start 2026-08-13, target end 2026-08-14, capacity M) with scope note + phase plan A–G in dependency order (A contracts from PRD §4.5/§5.9/§7.7-7.8/§7.9; B TailorDecider TASK-059/060; C bullet rewriting via `ai.generate(task='resume_tailoring')` TASK-061/062; D summary TASK-063; E reorder TASK-064; F `V5__create_tailoring_tables.sql` persistence TASK-065; G `TailoringController` + E2E TASK-066 → close CL-013). Locked decisions: AI per-bullet calls recorded in `ai_runs` (stub in dev, deterministic A/B/C-safe label `_stub`); validation is a disabled hook (SPRINT-05 adds `VALIDATING` edges); no chaining — references `analysis_id` + master `resume_version_id`; `score_before` = analysis score, `score_after` = deterministic MatchPipeline re-run; `html`/`pdf_path`/`docx_path` NULL until SPRINT-06; TASK-066 also ships `GET /tailored/{id}` (07 §3 maps it to FEAT-032). Marked EPIC-006 IN_PROGRESS; FEAT-028..032 + TASK-059..066 READY (`06-technical-tasks.md` status + `updated_at` 2026-08-13); counts regenerated in `05-backlog.md` (5 EPICs DONE / 1 IN_PROGRESS / 5 PLANNED, 27 FEATs DONE / 5 READY / 16 PLANNED, 58 TASKs DONE / 8 READY / 26 PLANNED) and `13-project-status.md` (§1/§2/§3/§5/§6); **fixed stale EPICs count line in `05-backlog.md` (said 3 DONE / 8 PLANNED — missed by CL-011; truth 5 DONE / 6 PLANNED / 1 BACKLOG)**. | Open the AI Tailoring sprint per the roadmap: planning closed, statuses truth-told before any implementation, recursive phase plan ready (TASK-059 next, IN_PROGRESS when implementation starts) | `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `15-change-log.md` | SPRINT-04 ACTIVE: 5 EPICs / 27 FEATs / 58 TASKs DONE, 1 EPIC / 5 FEATs / 8 TASKs READY; implementation begins with TASK-059 (TailorDecider). |
| CL-011 | 2026-08-13 | **SPRINT-03 (EPIC-004/005) implemented and verified — Deterministic Matching + Job Match Score**: `org.apache.commons:commons-text:1.12.0` added (FuzzyScore); matching core in `infrastructure/matching/` (`Normalizer` — lowercase/punctuation/whitespace normalization; `AliasTable` — whole-phrase + per-token canonicalization, js↔javascript, back-end↔backend, …; `ExactMatcher` EXACT/TOKEN/FUZZY grades with fuzzy ratio vs requirement length; `EvidenceIndex` — token→ids map + capped phrase search; `HybridMatcher` — keyword-first, phrase fallback, per-requirement matched/partial/unmatched; `MatchPipeline` — phased exact-first orchestration incl. JD keyword coverage, responsibility matching, seniority alignment; `SemanticMatcher` — disabled hook, `ats.doctor.matching.semantic-enabled=false`); `infrastructure/scoring/ScoreCalculator` (PRD §5.3 weights skills .30 / keywords .20 / responsibilities .20 / experience .15 / seniority .10 / education .05 × importance factors high 1.0 / medium 0.75 / low 0.5, partial counts 0.5, neutral 100 for empty categories, breakdown + strengths + deterministic suggestions, `total` key added so the persisted score reconstructs from JSON); `V4__create_analyses_table.sql` (`analyses` with `state`/`error`/`updated_at` — documented deviation from PRD §7.6 `status`); `Analysis` entity + repo; `StateMachines.analysis()` gained READY→QUEUED + FAILED→QUEUED (reanalyze); `AnalysisService` (queue validates job/resume READY, load builds `MatchRequest` from `job_requirements` rows + `resume_evidence` + education degrees, mark/completeSuccess/completeFailure, reanalyze resets results) + async `AnalysisPipeline` (`@Async @EventListener`, no AI calls — deterministic matching records nothing in `ai_runs`); `api/analyses/` (`POST /analyses`, `GET /analyses` list, `GET /analyses/{id}`, `POST /analyses/{id}/reanalyze`, snake_case `AnalysisResponse` with re-parsed JSONB, ProblemDetail advice 400/404/409); all beans gated on `ats.doctor.persistence.enabled`. Tests: 46 new (141 total, all green) incl. Testcontainers full-pipeline test (resume+JD → analysis READY → score 68, breakdown 6 categories + total, matches 5 with ≥80% matched, gaps, reanalyze round-trip, 400/404); docker-compose stack smoke-tested (analyze live Google JD vs Jane Doe resume → score 68, reanalyze recovers a legacy null-score row, error paths verified). Marked EPIC-004/005, FEAT-022..027, TASK-046..058 DONE; SPRINT-03 COMPLETED; counts 5 EPICs / 27 FEATs / 58 TASKs. | Ship the matching+scoring sprint: exact-first deterministic matching consuming `job_requirements` + `resume_evidence`, weighted 0–100 Job Match Score with explainable breakdown, `POST /analyses` pipeline + persistence + reanalyze, per 04-sprints exit criteria | `03-features.md`, `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `07-api-contract.md`, `08-database-schema.md`, `13-project-status.md`, `15-change-log.md`, `application.yml`, `backend/pom.xml`, `backend/*` | SPRINT-03 DONE: 5 EPICs / 27 FEATs / 58 TASKs DONE; analysis pipeline ready for SPRINT-04 (AI Tailoring). |
| CL-010 | 2026-08-13 | **SPRINT-02 (EPIC-003) implemented and verified — JD Intelligence**: `V3__create_job_tables.sql` (`jobs` with `state`/`error`/`temperature`/`updated_at` beyond PRD §7.4 — same precedent as `resume_versions` V2; `job_requirements` with type/importance CHECK constraints, keywords TEXT[], no pgvector — embedding deferred to FEAT-049); `Job`/`JobRequirement` entities + repos; `LocalFileStorage` gained a jobs root (`ats.doctor.storage.jobs-dir`, `data/jobs`); `JdParser` (extraction reuses the SPRINT-01 PDF/DOCX/TXT pipeline, `jd_parser` AI call); §4.3 `JobDto` with Bean Validation (code-fence tolerant, unknown fields ignored, `job.title` required); `RequirementExtractor` (deterministic normalization — lowercase/unique keywords, type/importance coalesced so CHECKs can't reject); `JobService` + async `JobPipeline` (event-after-commit, `StateMachines.job()` on every mutation, second AI call `requirement_extraction`); `JobController` (`POST /jobs` file **xor** text ≤10MB/≤1MB, `GET /jobs`, `GET /{job_id}`, `DELETE /{job_id}`) + ProblemDetail advice; `StubAiProvider` canned §4.3 JD aligned with the canned resume + canned requirements (labeled `_stub`); all beans gated on `ats.doctor.persistence.enabled`. Tests: 36 new (95 total, all green) incl. Testcontainers full-pipeline test (pasted + file JDs → READY → requirement rows → two `ai_runs` records → list/get/delete); docker-compose stack smoke-tested (create → READY → delete round-trip, error paths 400/404). Marked EPIC-003, FEAT-017..021, TASK-039..045 DONE; SPRINT-02 COMPLETED; counts 3 EPICs / 21 FEATs / 45 TASKs. | Ship the JD-intelligence sprint: paste/upload JD → extract → AI structure (§4.3) → requirements+keywords → persist, per 04-sprints exit criteria | `03-features.md`, `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `08-database-schema.md`, `13-project-status.md`, `15-change-log.md`, `application.yml`, `backend/*` | SPRINT-02 DONE: 3 EPICs / 21 FEATs / 45 TASKs DONE; JD pipeline ready for SPRINT-03 (Deterministic Matching + Job Match Score). |
| CL-009 | 2026-08-13 | **SPRINT-01 (EPIC-002) implemented and verified — Master Resume**: `V2__create_resume_tables.sql` (resumes / resume_versions with `state`+`error` / resume_evidence, no pgvector — embedding deferred to FEAT-049); entities+repos; `LocalFileStorage` (path-traversal-safe, `data/resumes/`); PDFBox 3.0.4 + POI XWPF extraction (`PdfParser`/`DocxParser`/`TextExtractionService`); §4.2 `ResumeDto` with Bean Validation (code-fence tolerant, unknown fields ignored); deterministic `EvidenceExtractor` (A/B/C rules, metadata, normalized text); `ResumeService` + async `ResumePipeline` (in-process executor, event-after-commit, `StateMachines.resume()` enforced on every mutation); `ResumeController` (`POST /resumes/upload` 10MB/type validation → 202-style immediate version at UPLOADED, `GET /current`, `GET /{id}`, `PUT /{id}/edit` re-validate in place) + ProblemDetail advice; `spring.mvc.problemdetails.enabled`; `StubAiProvider` now returns a schema-valid canned §4.2 resume (labeled `_stub`) so the pipeline works offline; frontend `resume/page.tsx` review UI (upload, state polling, evidence A/B/C counts, inline edit) + multipart/PUT API client. Tests: 34 new (59 total, all green) incl. Testcontainers full-pipeline test; `next build` green; docker-compose stack smoke-tested (upload→READY→edit round-trip); `.env` persistence enabled + datasource URL fixed (host `db`); compose passes `ATS_DOCTOR_PERSISTENCE_ENABLED`. Marked EPIC-002, FEAT-010..016, TASK-026..038 DONE. | Ship the master-resume sprint: upload → extract → AI structure → evidence → persist → review/edit, per 04-sprints exit criteria | `03-features.md`, `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `07-api-contract.md` (ProblemDetail note), `08-database-schema.md`, `13-project-status.md`, `15-change-log.md`, `.env`, `docker-compose.yml`, `backend/*`, `frontend/*` | SPRINT-01 DONE: 2 EPICs / 16 FEATs / 38 TASKs DONE; resume pipeline ready for SPRINT-02 (JD Intelligence). |
| CL-008 | 2026-08-12 | **SPRINT-00 (EPIC-001) implemented and verified**: backend (Spring Boot, health endpoint, Maven wrapper, Dockerfile, AI abstraction layer, facade with retry/fallback, prompts, stub + Spring AI gateway, state machines, ai_runs migration + recorder, /ai/config + /ai/models), frontend (Next.js 14 shell, Tailwind/shadcn, typed API client), docker-compose stack + .env.example, omniroute/config.yaml; `mvn test` green, frontend build + docker compose config verified, boot smoke check passed. Marked EPIC-001, FEAT-001..009, TASK-001..025 DONE; extended `.env.example` (`ATS_DOCTOR_AI_TIMEOUT_SECONDS`, `ATS_DOCTOR_AI_RETRY_MAX_ATTEMPTS`, `ATS_DOCTOR_AI_BACKOFF_BASE_MS`, `ATS_DOCTOR_AI_DEGRADE_TO_STUB`, `ATS_DOCTOR_PERSISTENCE_ENABLED`, `PROFILE_QUALITY_FALLBACK`); rebuilt truncated `03-features.md` from `05-backlog.md`/`06-technical-tasks.md`. | Complete the foundation sprint and keep docs truthful (statuses mirror reality) | `03-features.md`, `05-backlog.md`, `06-technical-tasks.md`, `08-database-schema.md`, `09-ai-specification.md`, `13-project-status.md`, `15-change-log.md`, `.env.example` | SPRINT-00 DONE: 1 EPIC / 9 FEAT / 25 TASK DONE; foundation ready for SPRINT-01. |
| CL-007 | 2026-08-12 | **Full documentation reconciliation to v1.2**: PRD §1.3/2.3/3/5/6/8/9/10/13/14 updated to the final stack; README → v1.2; DEC-023..033 recorded; all Python module paths, pytest/ruff, PyMuPDF/python-docx/WeasyPrint/Jinja2 references replaced with Java equivalents across docs 01–14; API contract updated to Spring `ProblemDetail`; Flyway note added to `08-database-schema.md`. | Keep every doc consistent with the finalized Java 21 / Spring Boot stack (source-of-truth hierarchy) | `README.md`, `01`–`14`, `ATS-Doctor-PRD.md` | Docs 01–15 and the PRD are mutually consistent on the v1.2 stack. |
| CL-006 | 2026-08-12 | **PRD bumped to v1.2 — final technology stack**: Python/FastAPI → **Java 21 + Spring Boot modular monolith** (Spring Web, Spring Data JPA + Hibernate, Spring AI, Flyway, Testcontainers; Apache PDFBox/POI, Thymeleaf + Flying Saucer/OpenPDF). Recorded as DEC-023..033. | Finalize architecture and stack decisions before scaffolding (Phase 2) | `ATS-Doctor-PRD.md`, `14-decision-log.md` | No more stack churn; phase 2 builds against v1.2. |
| CL-005 | 2026-08-12 | **Reality correction**: SPRINT-00 reset to greenfield. The scaffold claimed in CL-004 (docker-compose.yml, .env.example, .gitignore, data/, omniroute/config.yaml, backend/*) does **not** exist on disk — only `docs/` is present. Reset FEAT-001/002/004/005/006 and TASK-001..005, 008, 012..016, 018 from IN_PROGRESS to PLANNED; removed "file exists" notes from `03-features.md`; superseded DEC-022. | Docs must reflect reality; no phantom IN_PROGRESS | `03-features.md`, `04-sprints.md`, `05-backlog.md`, `06-technical-tasks.md`, `13-project-status.md`, `14-decision-log.md` | SPRINT-00 (EPIC-001, TASK-001..025) is entirely PLANNED; all 99 tasks now PLANNED or BACKLOG, 0 DONE. |
| CL-004 | 2026-08-12 | ~~Sprint 0 partial scaffold written~~ **SUPERSEDED by CL-005** (files never persisted) | Started implementation per PRD §14/Sprint 0 | `13-project-status.md`, `04-sprints.md` | Historical only — do not rely on. |
| CL-003 | 2026-08-12 | Living documentation set created: README + docs 01–15 | Establish status-updateable implementation docs with PRD v1.1 as source of truth | all `docs/*` | New doc IDs (EPIC/FEAT/TASK/SPRINT/DEC/CL). DEC-020..022 record the system's own decisions. |
| CL-002 | 2026-08-12 | PRD v1.1 migrated into `docs/ATS-Doctor-PRD.md` (from `doc/`); README link updated | Single source of truth colocated with the living docs | `docs/ATS-Doctor-PRD.md`, `docs/README.md` | All references use `docs/ATS-Doctor-PRD.md`. |
| CL-001 | 2026-08-12 | Initial status baseline recorded | Bootstrap of this documentation system | `05-backlog.md`, `13-project-status.md` | 12 EPICs / 53 FEATs / 99 TASKs catalogued; 0 DONE; SPRINT-00 ACTIVE. |

---

## Change record template

When recording a change, append a new row (next `CL-###`) with: date, what,
why, affected docs, impact. Never edit or delete historical rows.
